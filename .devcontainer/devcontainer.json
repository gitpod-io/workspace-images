{
  "name": "workspace-images",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "remoteUser": "gitpod",
  "containerUser": "gitpod",
  "workspaceFolder": "/workspace/workspace-images",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace/workspace-images,type=bind",
  "postCreateCommand": "NPX_PATH=$(which npx) && sudo rm -f \"$NPX_PATH\" && echo '#!/bin/sh' | sudo tee \"$NPX_PATH\" > /dev/null && echo 'echo \"npx is disabled for security reasons. Use explicit package installation instead.\" >&2' | sudo tee -a \"$NPX_PATH\" > /dev/null && echo 'exit 1' | sudo tee -a \"$NPX_PATH\" > /dev/null && sudo chmod +x \"$NPX_PATH\"",
  "forwardPorts": [
    5000
  ],
  "portsAttributes": {
    "5000": {
      "label": "Port 5000",
      "onAutoForward": "ignore",
      "elevateIfNeeded": true
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "timonwong.shellcheck"
      ]
    }
  }
}
